# install server meteor

- start to install nodeJs (stable version not de lastest)
- start to install meteor -> meteor.com -> .exe or curl the server
- On the terminal -> meteor create projet
- cd project
- npm install react react-dom redux node-sass
